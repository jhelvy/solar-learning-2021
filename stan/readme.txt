These are some early models for experimenting

model1 = basic one-factor LR model for the US
model2 = basic two-factor LR model for the US
model3 = same as model2, but log(cumCapacityKw) is implemented in the
         model3.stan file
model4 = same as model3, but now adding in lambda parameter
